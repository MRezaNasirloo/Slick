package ${packageName};

import com.mrezanasirloo.slick.SlickPresenter;

import javax.inject.Inject;

/**
 * A simple Presenter 
 */
public class Presenter${className} extends SlickPresenter<View${className}> {

    @Inject
    public Presenter${className}() {

    }
}
